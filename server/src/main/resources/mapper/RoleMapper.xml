<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 定义一个映射配置文件的根节点 -->
<!-- namespace 属性用来定义配置命名空间， 主要进行 session 级别的缓存管理 -->
<!-- namespace 默认使用当前操作实体类的全路径 -->
<mapper namespace="com.kiss.account.mapper.RoleMapper">

    <insert id="createRole" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.kiss.account.entity.Role">
        insert into roles(name, type, status, remark, operatorId, operatorName, operatorIp)
        values(#{name}, #{type}, #{status}, #{remark}, #{operatorId}, #{operatorName}, #{operatorIp});
    </insert>

    <select id="getRoleById" resultType="com.kiss.account.entity.Role">
        select * from roles where id=#{id};
    </select>

    <select id="getRoles" resultType="com.kiss.account.entity.Role">
        select * from roles;
    </select>

    <insert id="allocatePermissionToRole" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.kiss.account.entity.RolePermissions">
        insert into rolepermissions(roleId, permissionId, dataScope, operatorId, operatorName, operatorIp)
        values(#{roleId}, #{permissionId}, #{dataScope}, #{operatorId}, #{operatorName}, #{operatorIp});
    </insert>

    <insert id="allocatePermissionsToRole" useGeneratedKeys="true" keyProperty="id"
            parameterType="java.util.List">
        insert into rolepermissions(roleId, permissionId, dataScope, operatorId, operatorName, operatorIp)
        values
        <foreach collection="list" item="permission" index="index" separator=",">
            (#{permission.roleId}, #{permission.permissionId}, #{permission.dataScope}, #{permission.operatorId}, #{permission.operatorName}, #{permission.operatorIp})
        </foreach>
    </insert>

    <select id="getRolesPermissionIds" parameterType="java.lang.Integer" resultType="java.lang.Integer">
        select permissionId from rolepermissions where roleId=#{id};
    </select>

</mapper>